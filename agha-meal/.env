# Local backend over LAN — required for physical devices / Expo Go.
# Update the IP if your network changes (macOS: ipconfig getifaddr en0).
API_URL = http://192.168.1.180:8080

# Local backend via localhost — only works in the iOS simulator or `npm run web`
# API_URL = http://localhost:8080

# Production
# API_URL = https://foodapplication-o93v.onrender.com
# API_URL = https://aghamealorderservice.onrender.com
